"""WCWAA Scout pipeline — top-level package.

Importable package (src-layout under dev/src/). All submodules resolve via
``scout.*`` once the project is installed with ``pip install -e .`` from the
repo root. See AGENTS.md for the module map and run commands.
"""

__version__ = "4.0.0"
